# Dependencies
node_modules/
backend/node_modules/
frontend/node_modules/
package-lock.json (if you want to keep lock files, remove this; usually it is better to keep them, but some users prefer not)
# But common practice is to keep package-lock.json. I'll only ignore node_modules.

# Environment files
.env
.env.*
backend/.env
frontend/.env
*.env

# Build outputs
build/
dist/
frontend/build/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS Files
.DS_Store
Thumbs.db

# Agent/Tooling internal data
.gemini/
brain/
tmp/
frontend_log.txt
backend_log.txt
logs/
*.log
